# Pinned to the exact versions installed during development (via `pip freeze`).
# A fresh clone must install these exact versions so a newer release can't
# silently change agent/tool behavior underneath the demo.
google-adk==2.3.0
google-genai==2.10.0
python-dotenv==1.2.2
gradio==6.19.0
fastapi==0.139.0
uvicorn[standard]==0.49.0
python-multipart==0.0.32   # required by FastAPI for the /profile/upload endpoint
openpyxl==3.1.5
pytest==9.1.1
mcp==1.28.1
httpx==0.28.1        # imported directly by ui/app.py (the Gradio HTTP client)
pydantic==2.13.4     # imported directly by api/server.py (request/response models)
